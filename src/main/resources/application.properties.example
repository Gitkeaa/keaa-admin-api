spring.application.name=keaa-admin-api
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/keaa_admin?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=CHANGE_ME
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Downloads / Certificates: uploaded files are written under this directory (relative to the
# server's working directory). Catalogues and certificates can be a few MB, so lift the
# default 1 MB multipart cap.
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=26MB

# =====================================================================
#  JWT — dev secret. In production this MUST come from an env var,
#  not from this file. (Base64, >=32 bytes for HS256.)
# =====================================================================
app.jwt.secret=CHANGE_ME
app.jwt.expiration-days=7
app.jwt.cookie-name=keaa_token

# Session-cookie flags. THE VALUES BELOW ARE DEV ONLY (plain http on localhost).
#
# In production, pick the pair that matches where the admin frontend is served from:
#
#   Admin on this API's own domain, or a subdomain of it:
#     app.jwt.cookie-secure=true
#     app.jwt.cookie-same-site=Lax
#
#   Admin on a SEPARATE domain (Vercel/Netlify/another host) calling this API:
#     app.jwt.cookie-secure=true
#     app.jwt.cookie-same-site=None
#     ...because Lax is NOT sent on a cross-site fetch(credentials:'include'), so login would
#     succeed and every request after it would come back 401.
#
# Secure=true requires HTTPS, and SameSite=None requires Secure. That combination is checked at
# startup (AuthController.validateCookieConfig) and refused, rather than left to fail silently
# in the browser. Whichever pair you choose, the admin's origin must ALSO be listed in
# app.cors.allowed-origins at the bottom of this file.
app.jwt.cookie-secure=false
app.jwt.cookie-same-site=Lax

# =====================================================================
#  Outbound email — catalogue-download lead notifications.
#
#  OFF until spring.mail.host is set: with no host Spring creates no JavaMailSender, so
#  LeadMailer logs "not configured" and skips the send. The lead is still stored and still
#  appears in the console, so nothing is lost while these stay blank.
#
#  To switch it on, add the block below (env vars in production — never put real credentials
#  in this file). Gmail / Workspace needs an App Password, not the account password.
#
#    spring.mail.host=smtp.gmail.com
#    spring.mail.port=587
#    spring.mail.username=${MAIL_USERNAME}
#    spring.mail.password=${MAIL_PASSWORD}
#    spring.mail.properties.mail.smtp.auth=true
#    spring.mail.properties.mail.smtp.starttls.enable=true
#
#  app.mail.notify  who receives the alert (comma-separated for several)
#  app.mail.from    envelope sender; most providers require it to match the mailbox above
# =====================================================================
app.mail.notify=
app.mail.from=

cloudinary.cloud-name=keaa-assets
cloudinary.api-key=CHANGE_ME
cloudinary.api-secret=CHANGE_ME
# Where admin-uploaded assets land in the Cloudinary Media Library (must be the FULL nested path,
# else Cloudinary creates a separate top-level folder of the same leaf name).
cloudinary.products-folder=1.Keaa Assets/Keaa products
cloudinary.gallery-folder=1.Keaa Assets/Keaa Gallery
# Applicant CVs (raw files: PDF/doc) uploaded on the public careers form land here.
cloudinary.resume-folder=1.Keaa Assets/Keaa Resumes



# The React admin/site runs here in dev; CORS must allow it WITH credentials (so the
# httpOnly cookie is sent). Comma-separated, and each entry may use a port wildcard because
# SecurityConfig registers these with setAllowedOriginPatterns. This covers both hostnames
# (localhost AND 127.0.0.1 — a browser on one is a DIFFERENT origin from the other) on any
# local port, so the Vite dev server (5173) and a production preview (4173, etc.) both work.
# IN PRODUCTION replace this with the exact deployed origin, e.g.
#   app.cors.allowed-origins=https://admin.keaa-international.net
app.cors.allowed-origins=http://localhost:*,http://127.0.0.1:*
